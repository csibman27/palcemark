{{#each user}}
    <div class="box box-link-hover-shadow">
        <h3 class="title">
            {{firstName}}
            {{lastName}}
            <a href="/user/deleteuser/{{_id}}" class="button">
                <i class="fas fa-trash"></i>
            </a>
        </h3>
    </div>
{{/each}}
